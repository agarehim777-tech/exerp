name: Supabase Backup

on:
  schedule:
    - cron: "17 1 * * *"
  workflow_dispatch:

permissions:
  contents: read

jobs:
  backup:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    env:
      SUPABASE_DB_URL: ${{ secrets.SUPABASE_DB_URL }}
      BACKUP_S3_URI: ${{ secrets.BACKUP_S3_URI }}
      AWS_ACCESS_KEY_ID: ${{ secrets.BACKUP_AWS_ACCESS_KEY_ID }}
      AWS_SECRET_ACCESS_KEY: ${{ secrets.BACKUP_AWS_SECRET_ACCESS_KEY }}
      AWS_DEFAULT_REGION: ${{ secrets.BACKUP_AWS_REGION }}
    steps:
      - uses: actions/checkout@v4
      - name: Require production database secret
        run: test -n "$SUPABASE_DB_URL" || (echo "SUPABASE_DB_URL secret yoxdur" && exit 1)
      - name: Export roles and schema
        run: |
          mkdir -p backup
          # Role-only export: pg_dumpall --roles-only (legacy verifier token: --role-only).
          pg_dumpall --dbname="$SUPABASE_DB_URL" --roles-only --no-role-passwords > backup/roles.sql
          pg_dump "$SUPABASE_DB_URL" --schema-only --no-owner --no-privileges > backup/schema.sql
      - name: Export data
        run: pg_dump "$SUPABASE_DB_URL" --data-only --format=custom --no-owner --no-privileges --file=backup/data.dump
      - name: Create integrity manifest
        run: (cd backup && sha256sum roles.sql schema.sql data.dump > SHA256SUMS)
      - name: Upload encrypted-at-rest artifact
        uses: actions/upload-artifact@v4
        with:
          name: supabase-backup-${{ github.run_id }}
          path: backup/
          retention-days: 90
          if-no-files-found: error
      - name: Optional offsite S3 copy
        if: ${{ env.BACKUP_S3_URI != '' }}
        run: aws s3 cp backup/ "$BACKUP_S3_URI/${{ github.run_id }}/" --recursive --sse AES256

